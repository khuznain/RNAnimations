import React from 'react';

import {Box} from '@/atoms';

const SlidingCounter = () => {
  return (
    <Box bg="counterBackground" height={60} width={170} borderRadius="lg"></Box>
  );
};

export default SlidingCounter;
